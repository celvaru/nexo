class Favorito {
  constructor(id, fechaAgragado, usuarioId, propiedadId) {
    this.id = id
    this.fechaAgragado = fechaAgragado
    this.usuarioId = usuarioId
    this.propiedadId = propiedadId
  }
}

export default Favorito