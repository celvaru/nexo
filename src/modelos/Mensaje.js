class Mensaje {
  constructor(id, contenido, fecha, hora, chatId) {
    this.id = id
    this.contenido = contenido
    this.fecha = fecha
    this.hora = hora
    this.chatId = chatId
  }
}

export default Mensaje